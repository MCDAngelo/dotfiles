let mapleader = "\<Space>" "set the leader key to the comma key (alt use space)
nnoremap <leader>h :noh<CR> "toggle search highlighting
"map <leader>vd :call ToggleThemeMode('dark')<CR>
"map <leader>va :call ToggleThemeMode('')<CR>
